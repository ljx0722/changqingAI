name: Deploy to Sealos

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: changqingai-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: npm test
      - run: npm run build
      - name: Verify project structure
        run: |
          test -f index.html || (echo "Missing index.html" && exit 1)
          test -f nginx.conf || (echo "Missing nginx.conf" && exit 1)
          test -f Dockerfile || (echo "Missing Dockerfile" && exit 1)
          echo "All required files present."

  build:
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v4
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/changqingai:sha-${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/changqingai:latest

  deploy-sealos:
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-kubectl@v4
        with:
          version: latest
      - name: Configure Sealos cluster
        run: |
          mkdir -p "$HOME/.kube"
          echo "${{ secrets.SEALOS_KUBECONFIG }}" | base64 -d > "$HOME/.kube/config"
          kubectl config current-context
          ns="${{ secrets.SEALOS_NAMESPACE }}"
          kubectl get namespace "$ns" && echo "namespace OK: $ns"

      - name: Write deploy manifest and apply
        run: |
          IMAGE="ghcr.io/${{ github.repository_owner }}/changqingai@${{ needs.build.outputs.digest }}"
          echo "Deploying: ${IMAGE}"
          cat > /tmp/deploy.yaml << ENDMANIFEST
          apiVersion: v1
          kind: Service
          metadata:
            name: changqingai
            labels:
              app: changqingai
              cloud.sealos.io/app-deploy-manager: changqingai
          spec:
            ports:
              - port: 80
                targetPort: 8080
                name: http
                protocol: TCP
            selector:
              app: changqingai
          ---
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: changqingai
            annotations:
              originImageName: ghcr.io/${{ github.repository_owner }}/changqingai:latest
              deploy.cloud.sealos.io/minReplicas: '1'
              deploy.cloud.sealos.io/maxReplicas: '1'
            labels:
              app: changqingai
              cloud.sealos.io/app-deploy-manager: changqingai
          spec:
            replicas: 1
            revisionHistoryLimit: 1
            selector:
              matchLabels:
                app: changqingai
            strategy:
              type: RollingUpdate
              rollingUpdate:
                maxUnavailable: 0
                maxSurge: 1
            template:
              metadata:
                labels:
                  app: changqingai
              spec:
                automountServiceAccountToken: false
                containers:
                  - name: changqingai
                    image: ${IMAGE}
                    resources:
                      requests:
                        cpu: 100m
                        memory: 128Mi
                      limits:
                        cpu: 1
                        memory: 512Mi
                    ports:
                      - containerPort: 8080
                        name: http
                    imagePullPolicy: Always
                    readinessProbe:
                      httpGet:
                        path: /health
                        port: 8080
                      initialDelaySeconds: 3
                      periodSeconds: 5
                    livenessProbe:
                      httpGet:
                        path: /health
                        port: 8080
                      initialDelaySeconds: 10
                      periodSeconds: 15
          ENDMANIFEST
          kubectl apply -f /tmp/deploy.yaml

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/changqingai --timeout=180s
          kubectl get pods -l app=changqingai

      - name: Diagnostics on failure
        if: failure()
        run: |
          kubectl get deployment changqingai -o yaml | tail -40
          kubectl get rs -l app=changqingai
          kubectl get pods -l app=changqingai
          kubectl get events --sort-by=.lastTimestamp | tail -20
